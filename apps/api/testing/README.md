# Testing Structure

- `testing/unit` - unit tests for pure business logic
- `testing/integration` - endpoint-level integration tests
- `testing/fixtures` - reusable mock payloads and data
- `testing/helpers` - reusable test helpers/setup

## Run tests

```bash
npm test
npm run test:e2e
```
